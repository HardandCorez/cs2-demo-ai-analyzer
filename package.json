{
  "name": "hardandcore-cs2-demo-ai",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "CS2 demo analyzer with demoparser2, V6 positioning context and local Ollama coaching",
  "scripts": {
    "start": "node src/server.js",
    "start:ollama-gateway": "node src/ollama-gateway.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/v5-metrics.js && node --check src/v6-positioning.js && node --check src/ollama-gateway.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@laihoe/demoparser2": "^0.42.0",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
