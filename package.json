{
  "name": "hardandcore-cs2-demo-ai",
  "version": "4.0.1",
  "private": true,
  "type": "module",
  "description": "CS2 demo analyzer recovery build using the stable core parser and positioning pipeline",
  "scripts": {
    "start": "node src/server.js",
    "start:ollama-gateway": "node src/ollama-gateway-v62.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/v5-metrics.js && node --check src/v6-positioning.js && node --check public/radar-catalog.js && node --check public/bootstrap-v72.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@laihoe/demoparser2": "^0.42.0",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
